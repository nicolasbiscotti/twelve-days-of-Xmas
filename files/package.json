{
  "scripts": {
    "lint": "eslint --config ${CYBER_DOJO_SANDBOX}/.eslintrc.json /**/*.js",
    "test": "jest --coverage"
  },
  "jest": {
    "coverageReporters": [
      "text"
    ]
  },
  "devDependencies": {
    "jest": "^29.5.0"
  }
}
